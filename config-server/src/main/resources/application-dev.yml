server:
   port: 7000
management:
   endpoints:
      web:
         exposure:
            include: "*"
eureka:
   instance:
      preferIpAddress: true
      instanceId: ${spring.cloud.client.ip-address}:${server.port}
      hostname: ${spring.cloud.client.ip-address}
   client:
      serviceUrl:
         defaultZone: http://eureka:123654@localhost:7001/eureka
spring:
   security:
      user:
         name: config
         password: 123654
   rabbitmq:
      virtual-host: /renlm
      host: rabbitmq.renlm.cn
      port: 30672
      username: renlm
      password: MR1bi1Q^YhtMKit_0lcsmRa78Q^02dfs
   cloud:
      config:
         server:
            git:
               uri: https://gitee.com/renlm/config-repo
               forcePull: true
               defaultLabel: master