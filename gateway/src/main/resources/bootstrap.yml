spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    config:
      failFast: true
      uri: https://config-server.renlm.cn
      username: springCloud
      password: x654123
management:
  endpoints:
    web:
      exposure:
        include: '*'