server:
   port: 7001
spring:
   application:
      name: eureka-server
   profiles:
      active: dev
   config:
      import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:7000/}
management:
   endpoints:
      web:
         exposure:
            include: '*'