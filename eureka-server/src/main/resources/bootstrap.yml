server:
  port: 7001
spring:
  application:
    name: eureka-server
logging:
  level:
    root: TRACE
  file:
    name: /opt/log/eurekaServer.log
  logback:
    rollingpolicy:
      maxFileSize: 512MB
      maxHistory: 30