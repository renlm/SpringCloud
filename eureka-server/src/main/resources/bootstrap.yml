server:
  port: 7001
logging:
  level:
    root: info
  file:
    name: /opt/log/eurekaServer.log
  logback:
    rollingpolicy:
      maxFileSize: 512MB
      maxHistory: 30
spring:
  application:
    name: eureka-server