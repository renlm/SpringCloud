spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    config:
      failFast: true
      name: demo,eureka-client,redis,rabbitmq,logging,OAuth2ResourceServerJwt
      uri: ${CONFIG_URI:http://localhost:7000}
      username: ${CONFIG_USERNAME:springCloud}
      password: ${CONFIG_PASSWORD:123654}